import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.buildingqr',
  appName: 'Building QR',
  webDir: 'dist',
  backgroundColor: '#120b30',
  android: {
    backgroundColor: '#120b30',
  },
};

export default config;
